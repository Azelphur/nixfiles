return {
    left = os.getenv("MONITOR_LEFT"),
    right = os.getenv("MONITOR_RIGHT"),
    top = os.getenv("MONITOR_TOP"),
    bottom = os.getenv("MONITOR_BOTTOM"),
    simrig = os.getenv("MONITOR_SIMRIG"),
}
